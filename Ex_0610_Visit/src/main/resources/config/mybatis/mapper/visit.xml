<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="v">
	<!-- 방명록 전체 조회 -->
	<select id="visit_list" resultType="visit" parameterType="java.util.Map">
		select * from
		(select rank() over(order by idx DESC) no, v.* from VISIT v)
		where no BETWEEN #{start} and #{end}
	</select>
	<!-- 방명록 글쓰기 -->
	<insert id="visit_update" parameterType="visit">
		insert into VISIT values(
				seq_visit_idx.nextVal,
				#{name},
				#{content},
				#{pwd},
				#{ip},
				sysdate
				)
	</insert>
	
	<!-- 전체 글 수 -->
	<select id="rowCount" resultType="int">
		select count(*) from visit
	</select>
	
	<!-- 삭제를 위한 비밀번호 체크 -->
	<select id="select_one" resultType="visit" parameterType="int">
		select * from VISIT where idx=#{idx}
	</select>
	
	<!-- 게시글 삭제 -->
	<delete id="visit_delete" parameterType="int">
		delete from VISIT where idx=#{idx}
	</delete>
	
	<!-- 게시글 수정 -->
	<update id="visit_update" parameterType="visit">
		update VISIT set name=#{name}, content=#{content},
		pwd=#{pwd}, ip=#{ip}, regdate=sysdate
		where idx=#{idx}
	</update>
</mapper>












